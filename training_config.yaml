Training:
  model_path: 'yolov8n-pose.pt'  # Pre-trained model (n, s, m, l, x)
  epochs: 100
  img_size: 640
  batch_size: 16
  project: 'card_corner_detection'
  name: 'experiment_1'
  save_period: 10  # Save checkpoint every N epochs
  cache: true      # Cache images for faster training
  augment: true    # Enable data augmentation

Dataset:
  data_yaml_path: 'yolov8_pose_dataset/dataset.yaml'

Augmentations:
  # Geometric augmentations
  mosaic: 0.5        # Mosaic augmentation probability
  mixup: 0.1         # Mixup augmentation probability
  degrees: 90        # Rotation range (-degrees to +degrees)
  translate: 0.1     # Translation fraction of image size
  scale: 0.5         # Scale range (1-scale to 1+scale)
  shear: 2.0         # Shear angle range
  perspective: 0.1 # Perspective transformation
  
  # Flip augmentations
  flipud: 0.0        # Vertical flip probability (disabled for cards)
  fliplr: 0.5        # Horizontal flip probability
  
  # Color augmentations
  hsv_h: 0.015       # Hue augmentation range
  hsv_s: 0.7         # Saturation augmentation range  
  hsv_v: 0.4         # Value (brightness) augmentation range